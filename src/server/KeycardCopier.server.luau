local KeycardCopier = require(script.Parent.KeycardCopier)

-- Example hookup
-- local prompt = workspace.SomeDoor.ProximityPrompt
-- local remoteOrDoorLogic = ...

-- prompt.Triggered:Connect(function(player)
-- 	local character = player.Character
-- 	local tool = character and character:FindFirstChildOfClass("Tool")
--
-- 	KeycardCopier.tryUnlock(player, tool, function()
-- 		-- fire door unlock event here
-- 	end)
-- end)